# Active rig tooling

New source validators, mesh validators and preview renderers belong here.

Tools must follow the stage gates in
`res://assets/rc3/rig/rig_manifest.json`. The archived 29-Sprite2D validator is
not a template for the new mesh rig.
