# Active rig scenes

New deformable RC3 rig scenes belong here. The directory intentionally contains
no scene until `kotone_front_t_pose.png` is approved.

Do not restore or reference `res://archive/rc3_rigid_cutout/scene/`.
