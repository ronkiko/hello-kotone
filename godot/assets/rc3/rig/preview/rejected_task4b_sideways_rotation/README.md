# Rejected sideways-rotation previews

These previews were produced at commit `c7089ef` with localized joint weights,
but the renderer rotated the front-view calf by 20 and 45 degrees in the image
plane. A knee cannot flex sideways this way. The images are retained only as
failure evidence and do not represent the current depth-projection renderer.
